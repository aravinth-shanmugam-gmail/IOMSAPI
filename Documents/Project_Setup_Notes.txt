Note the swagger.json is obtained locally from the swagger-ui-dist package. 
        The swagger.json is generated by the swagger-jsdoc package and would be available in the url - https://localhost:7176/swagger/v1/swagger.json



Database:
===========
Since the new field added was having null values, the listall was erroring out so populated it with dummy values.

The repository is IOMSAPI from github.
Currently manual publishing is done to azure. (zip package is created and uploaded to azure portal))